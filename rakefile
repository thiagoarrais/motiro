require 'fileutils'

SITE_DIR = File.dirname(__FILE__)

task :copy_source do
    builddir = "#{SITE_DIR}/build"
    mkdir_p builddir
    #TODO remove this os dependency
    `cp -ru #{SITE_DIR}/src/* #{builddir}`
end

task :rails_generate => :copy_source do
    `rails -s #{SITE_DIR}/build`
end

task :default => :rails_generate

