<% @title = "Motiro - #{'Event'.t}" %>
<% if ! @partial then %>
      <div id="description" class="window">
        <div class="inner">
          <h1><%= h(@headline.title(Translator.for(@locale))) -%></h1>
<% end %>
<%= page = Page.new
    page.text = @headline.description(Translator.for(@locale))
    page.render_html %>
<% if ! @partial then %>
        </div>
      </div>
<% end %>

    
