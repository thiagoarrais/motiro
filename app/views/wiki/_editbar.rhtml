<div class='editbar'>
  <% if @session[:user] then%>
      <%= link_to( 'Editar',
                   { :controller => 'wiki',
                     :action => 'edit',
                     :page => @page.name } )%>
  <% else %>
      <span class='disabled'>Editar (identifique-se, por favor)</span>
  <% end %>
</div>
